# CPU-only torch wheels (no CUDA): the extra index serves the "+cpu" builds,
# which PEP 440 ranks above the same PyPI release, so plain
# `pip install -r requirements.txt` also stays CPU-only.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
defusedxml>=0.7,<1
psycopg[binary]>=3.2,<4
redis>=5,<7
httpx>=0.27,<1
torch>=2.2,<3
sentence-transformers>=3,<6
